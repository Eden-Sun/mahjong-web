# 🀄 mahjong-web

台灣麻將遊戲，純前端實作。TypeScript + WebAssembly (Go)，無需後端。

**[▶ 線上遊玩](https://eden-sun.github.io/mahjong-web/)**

---

## 功能

- 🎮 4 人對戰（1 人類 + 3 AI）
- 🀄 台灣麻將規則（16 張、吃碰槓胡）
- ⚡ 遊戲邏輯跑在 WebAssembly（Go 編譯）
- 🃏 完整動作支援：吃、碰、明槓、暗槓、加槓、搶槓、自摸、放砲
- 🔢 天聽模式：固定手牌 `1112345678999萬 發發發`，方便測試胡牌邏輯
- 📱 RWD，桌機 / 手機皆可

## 技術架構

| 層次 | 技術 |
|------|------|
| UI | TypeScript + Vite |
| 遊戲引擎 | Go → WebAssembly |
| 測試 | Vitest（73 個測試） |
| 部署 | GitHub Pages |

## 本地開發

```bash
# 安裝依賴
npm install

# 啟動 dev server
npm run dev

# 跑測試
npm test

# Build
npm run build
```

Dev server 預設：`http://localhost:5173/mahjong-web/`

## 專案結構

```
src/
├── main.ts           # 進入點、UI、選單
├── gameController.ts # 回合流程控制
├── gameState.ts      # 遊戲狀態定義
├── actionChecker.ts  # 吃碰槓胡合法性檢查
├── winChecker.ts     # 胡牌判斷（接 WASM）
├── aiLogic.ts        # AI 出牌邏輯
├── tileRenderer.ts   # 牌面渲染
└── wasm.ts           # WASM 橋接層
public/
└── game.wasm         # 編譯好的 Go 遊戲引擎
```

## 遊戲模式

### 一般模式
隨機發牌，正常對局流程。

### 天聽模式
玩家初始手牌固定為 `1112345678999萬 發發發`（天聽狀態），摸牌即可測試各種胡牌、搶槓等邏輯。

## License

MIT
